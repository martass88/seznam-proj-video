{
  "extends": ["stylelint-config-standard-scss", "stylelint-config-sass-guidelines"],
  "ignoreFiles": ["!**/*"],
  "overrides": [
    {
      "files": ["**/*.scss"],
      "rules": {
				"selector-class-pattern": null,
				"max-nesting-depth": 4
			}
    }
  ]
}
